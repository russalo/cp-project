# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual Environment
.venv/
venv/
backend/.venv/

# Environmental files
ENV/
env/
.env
.env.*
!backend/.env.example

# PyCharm
.idea/

# VS Code
.vscode/

# Local AI/dev-setup drafts (not project policy)
/ANTIGRAVITY.md
/docs/reference/ai-architecture.md

# Django
*.log
db.sqlite3
db.sqlite3-journal
/media
/staticfiles

# Local reference material / office temp files
.github-copilot/.~lock*
.github-copilot/*.xlsx
.github-copilot/*.pdf

# Local scratch / working material (reference only, not committed)
/scratch/

# Dev-only CP brand fonts — Monotype desktop EULA does not permit web
# hosting. Loaded via import.meta.glob in src/main.jsx when the files
# exist locally; absent on CI / clean clones (graceful fallback).
/frontend/src/assets/fonts-dev/
/frontend/src/assets/fonts-dev.css

# OS
.DS_Store
Thumbs.db

# WSL Windows NTFS alternate data stream artifacts
*:Zone.Identifier